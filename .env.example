# SMTP 이메일 발송 설정 — 이 파일을 복사해서 .env로 저장 후 실제 값을 채우세요
# cp .env.example .env

# 발송 이메일 계정 (Gmail 예시)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# 회원가입 허용 도메인 (@ 뒤 부분만)
ALLOWED_DOMAIN=cafegate.co.kr
