# 실제 거래 데이터 — 절대 Git에 포함하지 않음
data/raw/
*.xlsx
*.xls
*.csv

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# 가상환경
.venv/
venv/
env/

# Streamlit 캐시 (로컬 캐시만 제외, secrets.toml은 포함)
.streamlit/cache/

# 가입 대기 목록 (동적 파일, 로컬 전용)
pending_users.yaml

# 환경변수 (SMTP 비밀번호 포함)
.env

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
